require 'spec_helper'

describe SchemaDocsGenerator do
  it 'has a version number' do
    expect(SchemaDocsGenerator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
